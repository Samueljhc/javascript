const [a, b, c ,d] = [3, 5, 1, 15]

const adicao = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(adicao, subtracao, multiplicacao, divisao, modulo)


// ++d / prefixo / antes do operando
// op1 + op2 / infix / operador binário